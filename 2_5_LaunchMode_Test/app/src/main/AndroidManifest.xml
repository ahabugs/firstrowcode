<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="activity.example.com.a2_5_launchmode_test">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".FirstActivity"
            android:label="This is FirstActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SecondActivity"
            android:label="This is SecondActivity"
            android:launchMode="singleTop"></activity>
        <activity
            android:name=".OtherActivity"
            android:label="This is OtherActivity"></activity>
        <activity
            android:name=".SingleTaskActivity"
            android:label="This is SingleActivity"
            android:launchMode="singleTask"
            >

        </activity>
        <activity
            android:name=".SingleInstanceActivity"
            android:label="This is SingleInstanceActivity"
            android:launchMode="singleInstance"
            >
        </activity>
    </application>

</manifest>