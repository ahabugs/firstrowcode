<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="activity.example.com.a3_5listviewtest">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".FirstActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TextOnlyActivity" />
        <activity android:name=".CustomizedListViewActivity" />
        <activity android:name=".RecyclerViewActivityVERT" />
        <activity android:name=".RecyclerViewActivityHORI" />
        <activity android:name=".FruitStaggerLayoutActivity"></activity>
    </application>

</manifest>